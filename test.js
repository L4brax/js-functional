var assert = require('chai').assert;
let test = false;

describe('leTEST', function() {
	it('On espère que la valeur de test est bien vrai !?', function() {
		assert.equal(test,true, 'La valeur vrai n\'est pas VRAI !');
	});
});
